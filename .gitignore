#Git 저장소에는 Java 파일과 설정 파일만 올리기때문에
#그외 파일이 저장된 폴더는 제외 합니다
.gradle/
build/
bin/

#Visual Studio Code의 설정 파일은 제외 합니다
.vscode/

#파일에는 데이터베이스 URL, 계정, 비밀번호 같은 민감한 정보가 포함될 수 있습니다.
#이 정보는 노출되면 안 되므로 Git 추적에서 제외합니다.

src/main/resources/application.properties